let score = 299

console.log(score);

let balance = new Number(100);

// now due to this new Number it will the number 100 % and it has some property 

// like we can convert it into string and also assume that there is big calculation in making something 
// so we can use tofixed() to fix a number after decimal 

console.log(balance.toString());
console.log(balance.toFixed(2));
